{
  // This file is intentionally left mostly empty to avoid conflicts with
  // the serverless function configuration defined in the /api directory.
  // Vercel will automatically handle functions in the /api directory.
}